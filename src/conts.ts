export enum AppRoute {
  Root = '/',
  Friends = '/friends',
  Shelf = '/shelf',
  Storage = '/storage',
  Profile = '/profile',
  Book = '/book',
  Login = '/login',
  Register = '/register',
}

export type PropsWithClass = {
  customClass?: string;
}